<h1>Listing Itens compra</h1>

<table>
  <thead>
    <tr>
      <th>Quantidade</th>
      <th>Valor unitario</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for item_compra <- @itens_compra do %>
    <tr>
      <td><%= item_compra.quantidade %></td>
      <td><%= item_compra.valor_unitario %></td>

      <td>
        <%= link "Show", to: Routes.item_compra_path(@conn, :show, item_compra) %>
        <%= link "Edit", to: Routes.item_compra_path(@conn, :edit, item_compra) %>
        <%= link "Delete", to: Routes.item_compra_path(@conn, :delete, item_compra), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Item compra", to: Routes.item_compra_path(@conn, :new) %></span>
