<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :estado_id %>
  <%= select f, :estado_id, @estados %>
  <%= error_tag f, :estado_id %>

  <%= label f, :nome %>
  <%= text_input f, :nome %>
  <%= error_tag f, :nome %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
