<div class="default-form"> 
    <h1 class="h1">Nova Venda</h1>

    <%= render "form.html", Map.put(assigns, :action, Routes.venda_path(@conn, :create)) %>

    <span><%= link "Voltar", to: Routes.venda_path(@conn, :index), class: "btn" %></span>
</div>